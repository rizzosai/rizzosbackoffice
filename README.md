# Python Project

This is a minimal Python project scaffold. Includes a simple app, requirements.txt, and a test folder.

## Quick Start

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
2. Run the app:
   ```bash
   python app.py
   ```
3. Run tests:
   ```bash
   python -m unittest discover tests
   ```
